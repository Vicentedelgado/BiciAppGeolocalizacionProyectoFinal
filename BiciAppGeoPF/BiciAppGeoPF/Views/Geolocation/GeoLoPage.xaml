<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BiciAppGeoPF.Views.Geolocation.GeoLoPage"
             BackgroundColor="#48416B">
    <StackLayout>
        <StackLayout HorizontalOptions="Center"
                     VerticalOptions="Start">

            <Label Margin="0,0,0,10"
                   Text="Geolocation"
                   FontAttributes="Bold"
                   FontSize="Large"
                   TextColor="#CA6F1E"
                   HorizontalTextAlignment="Center"></Label>

            <Label Text="Tu localización"
                   VerticalOptions="Center"
                   HorizontalOptions="Start"
                   FontSize="45"
                   Margin="0,20,0,0"
                   TextColor="LightBlue" />

            <Button x:Name="btnLocation"
                    Text="Iniciar"
                    Clicked="btnLocation_Clicked" />

            <Label HorizontalTextAlignment="Center"
                   x:Name="lblLatitude"></Label>
            <Label HorizontalTextAlignment="Center"
                   x:Name="lblLongitude"></Label>

            <Button x:Name="btnOK"
                    Text="Parar"
                    Clicked="btnStop_Clicked" />
            <Button x:Name="btnStop"
                    Text="Ok"
                    Clicked="btnOK_Clicked" />
            <Button x:Name="btnFB"
                    Text="Guardar ubicacion"
                    Clicked="btnFB_Clicked" />

        </StackLayout>
    </StackLayout>
</ContentPage>